Environment Setup
`Install JDK 8 and Maven`


Deploy Localy
`mvn spring-boot:run`

Deploy G-cloud
`mvn appengine:deploy`

G-claud commands
`% gcloud auth login`
`% gcloud config set project YOUR_PROJECT_ID`
